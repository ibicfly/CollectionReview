jdk1.8
new ArrayList(capacity);
根据大小创建了一个对象数组
add(index,obj);
在添加元素的时候，如果添加后的元素大小超过了初始化时的大小
则将集合*3/2+1
在添加的时候可以选择添加的index
如果超过先有元素的个数-1则报错
remove(obj);
遍历集合匹配obj

ArrayList基于数组实现，无容量限制
插入元素时可能要扩容，删除元素不减少数组容量，在查找元素时需要遍历，采用equals进行比较
非线程安全

